<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center"
    android:elevation="6dp"
    android:padding="@dimen/default_btn_height"
    tools:context="com.pcamilo.loadingbutton.MainActivity">

    <com.pcamilo.loadingbutton.LoadingButton
        android:id="@+id/btnLoading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:buttonColor="@color/colorAccent"
        app:buttonLabel="@string/label_test"
        app:isLoading="true"
        app:loadingColor="@android:color/white"
        android:layout_centerInParent="true"/>

    <Button
        android:layout_below="@+id/btnLoading"
        android:layout_marginTop="@dimen/default_btn_height"
        android:id="@+id/startBtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Execute"
        android:layout_centerInParent="true"
        app:layout_constraintBottom_toBottomOf="parent" />


</RelativeLayout>